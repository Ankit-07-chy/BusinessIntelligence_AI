import { ComingSoon } from "../components/layout/ComingSoon";

export function InsightsPage() {
  return (
    <ComingSoon
      title="Insights"
      description="Anomaly list with materiality, confidence, and driver contribution — pending the analytics engine."
    />
  );
}
