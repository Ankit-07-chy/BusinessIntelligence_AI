# Used by docker-compose.yml to provision the local PostgreSQL container.
# Backend and frontend each keep their own .env.example with app-level settings.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=kpi_intelligence
POSTGRES_PORT=5432
