import { ComingSoon } from "../../components/layout/ComingSoon";

export function UsersPage() {
  return (
    <ComingSoon
      title="Admin · Users"
      description="Demo user management — pending the admin user management endpoint."
    />
  );
}
