import { ComingSoon } from "../components/layout/ComingSoon";

export function ActionsPage() {
  return (
    <ComingSoon
      title="Actions"
      description="Driver → lever → action → owner → impact recommendations — pending the action recommendation engine."
    />
  );
}
